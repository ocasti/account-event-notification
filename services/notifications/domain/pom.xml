<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>co.cobre</groupId>
    <artifactId>notifications</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>domain</artifactId>
  <name>domain</name>
  <description>Pure Java domain model: NotificationEvent, Subscription, DeliveryAttempt, RetryPolicy. No framework dependencies.</description>

  <!-- Aside from the test toolkit inherited from the parent (JUnit 5, AssertJ, Mockito),
       ArchUnit checks test-code conventions and the domain depends on nothing but the JDK. -->
  <dependencies>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Publishes domain/src/test as a test-jar so application and infrastructure can reuse
           the shared fixtures (co.cobre.notifications.domain.fixtures) in their own tests. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <execution>
            <id>test-jar</id>
            <goals>
              <goal>test-jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
