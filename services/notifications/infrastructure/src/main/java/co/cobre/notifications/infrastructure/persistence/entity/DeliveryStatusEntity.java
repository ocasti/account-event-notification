package co.cobre.notifications.infrastructure.persistence.entity;

/**
 * Delivery status enum for persistence.
 */
public enum DeliveryStatusEntity {
    REGISTERED, SCHEDULED, DELIVERED, FAILED, SKIPPED
}
